# Development uses SQLite (file-based)
DATABASE_URL="file:./prisma/dev.db"

# For Postgres in production, replace with something like:
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DB?schema=public"