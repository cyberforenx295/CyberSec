[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"

# Optional: redirect all unknown routes to the 404 produced by Next export
[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404